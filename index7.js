// Completa la función que tomando dos números como argumento devuelva el más alto.
const sum = (numberOne, numberTwo) => (numberOne > numberTwo ? numberOne : numberTwo);

  
  // let resultado = sum(5, 10);
  let result = sum(30, 14);
  console.log(result);